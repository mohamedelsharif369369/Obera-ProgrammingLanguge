# Variables & Arithmetic
x = 42
y = x + 8
print y

# Functions
func add(a, b):
    print a + b

add(5, 7)

# Classes
class Person:
    func greet(self):
        print "Hello from OBERA"

p = new Person
p.greet()

# Module import (ستحتاج لإنشاء module obr منفصل)
# import mymodule
# from mymodule import some_func
